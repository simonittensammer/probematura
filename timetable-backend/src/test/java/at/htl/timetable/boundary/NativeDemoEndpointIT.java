package at.htl.timetable.boundary;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeDemoEndpointIT extends DemoEndpointTest {

    // Execute the same tests but in native mode.
}